import { StyleSheet } from "react-native";

export const MyColors = {
  primary: "#0B993D",
  secondary: "#70ff8b",
  background: "#EEEEEE",
};

export const MyStyles = StyleSheet.create({
  loading: {
    bottom: 0,
    position: "absolute",
    top: 0,
    right: 0,
    left: 0,
  },
});
